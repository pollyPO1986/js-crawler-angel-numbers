// 數字 Mapping list
const ANGEL_NUMBERS = {
  ONE_DIGITS: ['0', '1'],
  TWO_DIGITS: [],
  THREE_DIGITS: [],
  FOUR_DIGITS: [],
};

export const numberGenerator = (digit, length) => {
  const aa = Array(length).fill(0).map((value, index) => )

  // let array1to5 = Array.from({ length: 5 }, (val, index) => index + 1); // [1, 2, 3, 4, 5]
};
